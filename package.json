{
  "name": "shopping-list-app",
  "version": "1.0.0",
  "description": "Real-time shopping list with auto-categorization",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "deploy": "git add . && git commit -m 'Deploy updates' && git push && vercel --prod"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.74.0",
    "dotenv": "^17.2.3",
    "express": "^4.18.2",
    "socket.io": "^4.6.1"
  }
}
